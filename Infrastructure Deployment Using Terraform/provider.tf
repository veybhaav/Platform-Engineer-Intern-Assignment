terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 4.0"
    }
  }
}

provider "aws" {
  region = "us-east-1"
  # Use "localstack" for local environment
  # endpoint_url = "http://localhost:4566"
}
